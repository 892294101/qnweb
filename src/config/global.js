/*
import {getCurrentInstance} from "vue"

const {proxy} = getCurrentInstance()

export default function globalApi() {

    const {appContext: {config: {globalProperties}}} = proxy
    return {...(globalProperties)}
}
*/


export default function discard() {

}